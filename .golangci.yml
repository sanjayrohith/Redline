version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - revive
  settings:
    revive:
      rules:
        - name: exported
          disabled: false
  exclusions:
    generated: lax

formatters:
  enable:
    - gofmt
    - goimports
